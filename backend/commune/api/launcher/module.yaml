module: 'api.launcher.module.Launcher'
name: launcher
# clients
client: get_cfg(client.manager)
max_actor_count: 4
# shifter: get_cfg(process.shifter.module)
actor:
  name: local_copy(name)
  max_concurrency: 4